# 마지막 과제 BR-B 제출문

## 앱 한 문장
반복적인 포함 여부 검색을 구현하는 JavaScript 개발자가, 과제 10 논문에서 직접 측정한 `Array.includes()`와 `Set.has()`의 결과를 비교해 **해당 실험 조건에서 어떤 방식이 더 짧게 관찰됐는지** 확인하도록 돕는 앱입니다.

## 사용자가 할 일 세 가지
1. `검색 자체 비교` 또는 `Set 생성 비용 + 100회 검색 비교` 중 하나를 고른다.
2. 데이터 원소 수를 입력한다. 논문에서 실제로 실험한 값은 10 / 100 / 1,000 / 10,000 / 100,000이다.
3. `실험 결과 비교하기`를 눌러 실제 측정값과 관찰 결과를 확인한다.

## 결과물 URL
https://br-b-js-structure-advisor-1.onrender.com/

## 짧은 확인 방법
1. 공개 URL `https://br-b-js-structure-advisor-1.onrender.com/`을 열어 첫 화면이 표시되는지 확인한다.
2. 상황을 `배열에서 시작 — Set 생성 비용 + 100회 검색 비교`, 원소 수를 `100`으로 입력한다.
   - Array 100회 `0.008438 ms`, Set 생성+100회 `0.008652 ms`, 관찰 결과 `Array.includes`가 나오면 통과.
3. 같은 상황에서 원소 수를 `1000`으로 바꾼다.
   - Array 100회 `0.069918 ms`, Set 생성+100회 `0.034491 ms`, 관찰 결과 `Set.has`가 나오면 통과.
4. 상황을 `이미 Array와 Set이 준비됨 — 검색 자체만 비교`, 원소 수를 `10000`으로 입력한다.
   - Array `7194.93 ns`, Set `6.51 ns`, 관찰 결과 `Set.has`가 나오면 통과.
5. 원소 수를 `0`, 빈 값, `500`처럼 바꿔 본다.
   - `0`/빈 값은 입력 오류 안내, `500`은 논문에서 측정하지 않은 크기라는 안내가 나오고 앱이 멈추지 않으면 통과.
6. 화면 아래 표에 논문의 다섯 실험 크기와 측정값이 표시되면 통과.

## AI와 나의 판단 세 줄
### ① AI에게 맡긴 일
논문의 실험 결과를 읽고 앱 구조로 바꾸는 초안 작성, HTML/CSS/JavaScript 구현, 입력 범위 검토, README와 제출용 문서 정리를 맡겼다.

### ② 내가 직접 판단한 일
과제 내용에 적합하지 않은 내용이 있는지 다시 검토하도록 요청했고, 논문에서 실제로 측정하지 않은 조건을 결과처럼 보여 주면 안 된다는 기준으로 최종 결과물을 수정하기로 했다.

### ③ AI 제안을 따르지 않은 일
초기 버전의 `임의 데이터 크기·검색 횟수 입력 → 가장 가까운 실험값으로 추천` 방식은 논문이 직접 검증하지 않은 범위까지 판단하는 문제가 있어 사용하지 않았다. 최종 버전은 논문에서 실제로 측정한 두 시나리오와 다섯 크기에만 결과를 제시하고, 그 밖의 크기에는 추정하지 않는다는 안내만 보여 주도록 제한했다.

## 동료 한 줄 감상
**[제출 전 실제 동료 한 사람에게 설명 없이 사용하게 한 뒤, 받은 한 줄 감상을 이름 없이 여기에 입력]**

> 이 항목은 실제 사용자의 확인 증거가 필요한 과제 조건이므로 임의의 감상을 작성하지 않았습니다.

## 12번 사이트 대표작 자리용 문구
**JavaScript Membership Advisor**  
과제 10 논문에서 측정한 `Array.includes()`와 `Set.has()`의 검색 성능과 Set 생성 비용을 실제 개발 판단 화면으로 옮긴 웹앱입니다. 논문이 직접 측정한 실험 조건을 선택하면 두 방식의 실제 측정값과 관찰 결과를 확인할 수 있습니다.

앱 링크: **https://br-b-js-structure-advisor-1.onrender.com/**
